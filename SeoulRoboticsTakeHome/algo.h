#pragma once
#include <iostream>
#include <vector>
#include <set>

int MirrorsAndRasors(std::vector<std::set<std::pair<int, int>>> row_mirrows, std::vector<std::set<std::pair<int, int>>> col_mirrows, int len_row, int len_col, std::vector<std::vector<int>>& add_mirrows);

